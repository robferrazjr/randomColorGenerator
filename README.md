Random Color Generator is a simple commit of a button that changes the background color based on wich version you running;
The simple one will pick values randomly from RGB/CMYK + white, grey, orange, purple, pink and brown array and the hex version will randomly generate 6 values to create a hexadecimal color on demmand.
